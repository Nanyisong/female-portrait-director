# skill/core/output-format.md

# 女性人像提示词导演 Skill｜输出格式规则

版本编号：`FEMALE-PORTRAIT-DIRECTOR-V1.4`
文档类型：核心规则文档
适用范围：提示词生成、参数组合推荐、提示词优化、失败诊断、审查友好改写、图片反推提示词、Skill 文档输出
核心职责：统一所有输出格式，确保输出内容可复制、可执行、结构稳定、不混乱

---

## 1. 文档定位

本文件用于规定女性人像提示词导演 Skill 的所有输出格式。

它负责解决以下问题：

1. 不同任务输出格式不统一；
2. 提示词生成时只复述参数，没有形成完整画面；
3. 参数组合推荐不够完整，无法直接调用；
4. 提示词优化只给建议，不给最终可复制版本；
5. 失败诊断只分析问题，不给修复结果；
6. 审查友好改写过度删减用户画面目标；
7. 图片反推提示词只描述图片，不输出可用提示词；
8. Skill 文档输出结构不一致；
9. 正向提示词和负面约束混在一起；
10. 输出内容过于摘要化，无法直接用于生成图片。

本文件必须被以下模块继承：

```text
skill/skill.md
skill/style-registry.md
skill/core/parameter-lock.md
skill/core/safety-boundary.md
skill/core/conflict-resolution.md
skill/core/fallback-rules.md
skill/references/director-expansion.md
skill/routes/
skill/overlays/
skill/tools/
```

---

## 2. 总输出原则

所有输出必须遵守以下原则：

```text
1. 用户要求生成提示词时，必须直接输出可复制提示词；
2. 用户要求完整模式时，必须包含参数锁定结果、最终提示词、负面约束；
3. 用户要求只输出提示词时，不额外分析；
4. 用户要求推荐参数组合时，每组参数必须完整可调用；
5. 用户要求优化时，必须给出优化后的最终版本；
6. 用户要求诊断时，必须指出问题、原因、修改方式，并尽量给出修复版；
7. 用户要求审查友好改写时，必须保留原画面目标；
8. 用户要求图片反推提示词时，必须先分析再输出可复制提示词；
9. 用户要求 Skill 文档时，一次只输出一个核心文档；
10. 不得只写摘要，不得只写说明，不得让用户二次整理后才能使用。
```

---

## 3. 输出模式总览

本 Skill 支持以下输出模式：

```text
1. 完整提示词模式
2. 只输出提示词模式
3. 参数组合推荐模式
4. 提示词优化模式
5. 失败诊断模式
6. 审查友好改写模式
7. 图片反推提示词模式
8. 多风格版本模式
9. Skill 文档输出模式
10. 简短说明模式
```

系统必须先识别用户任务类型，再选择对应输出格式。

---

# 4. 完整提示词模式

## 4.1 适用场景

当用户要求：

```text
根据参数生成提示词
输出完整提示词
生成一条可复制提示词
帮我写成完整生图提示词
根据这个风格生成提示词
```

默认使用完整提示词模式。

---

## 4.2 输出格式

```text
一、参数锁定结果

写真风格：
场景方向：
服装方向：
气质标签：
五官方向：
身形方向：
身形吸引力强度：
线条重点：
镜头方向：
光线氛围：
滤镜效果：
画幅比例：
平台用途：
补充要求：

二、最终提示词

……

三、负面约束

……
```

---

## 4.3 参数锁定结果填写规则

### 写真风格

填写用户明确指定或系统路由判断后的风格。

示例：

```text
写真风格：纯欲曲线生活照
```

### 场景方向

必须保留用户原场景，不得替换。

示例：

```text
场景方向：海边步道
```

### 服装方向

必须保留用户原服装，不得改品类、颜色、版型。

示例：

```text
服装方向：雾蓝色短款吊带 + 白色轻薄开衫 + 浅色短裤
```

### 气质标签

保留用户气质，并可补充少量相容词。

示例：

```text
气质标签：清纯、纯欲、安静、有吸引力
```

### 五官方向

如果用户未填写，根据 route 默认补全。

示例：

```text
五官方向：清冷淡颜
```

### 身形方向

如果用户未填写，根据 route 默认补全。

示例：

```text
身形方向：自然吸引力曲线
```

### 身形吸引力强度

如用户未填写，纯欲曲线类默认“中”，其他风格可写“不特别强调”。

示例：

```text
身形吸引力强度：中
```

### 线条重点

如用户未填写，按风格补全；如不适用，写“自然比例，不特别强调”。

示例：

```text
线条重点：脖颈、锁骨、腰线、小腹、大腿
```

### 镜头方向

保留用户镜头；缺失时按 route 补全。

示例：

```text
镜头方向：轻侧身站姿，大腿以上构图
```

### 光线氛围

保留用户光线；缺失时按 route 补全。

示例：

```text
光线氛围：傍晚柔和自然光
```

### 滤镜效果

保留用户滤镜；缺失时按 route 补全。

示例：

```text
滤镜效果：冷白纯欲生活照滤镜
```

### 画幅比例

如用户未填写，默认 9:16。

示例：

```text
画幅比例：9:16
```

### 平台用途

如用户未填写，可根据语境补全为“生活照 / 社交媒体 / 小红书种草 / 作品集”。

示例：

```text
平台用途：小红书种草
```

### 补充要求

必须保留用户原补充要求。

示例：

```text
补充要求：服装完整清晰，不要低俗，不要复杂背景
```

---

## 4.4 最终提示词写法规则

最终提示词不能机械复述字段，而是要根据导演规则进行详细的描述或扩写。

错误写法：

```text
生成一张纯欲曲线生活照，场景是海边步道，服装是雾蓝色短款吊带，气质是清纯、纯欲、安静、有吸引力，镜头是大腿以上构图，光线是傍晚自然光。
```

正确写法：

```text
生成一张 9:16 竖版、写实电影生活剧照感的纯欲曲线女性生活照。傍晚的海边步道上，画面主体是一位 20–28 岁年轻成年东方女性，成年气质明确，具有自然东方女性特征，不过度欧美化，不幼态化，没有未成年感。她在散步中短暂停下，身体轻微侧向镜头，重心自然落在后侧腿上，肩颈放松，眼神安静地看向远处海面。

人物五官方向为清冷淡颜，脸型柔和流畅，眉眼清淡，眼神克制自然，妆容清透干净，不网红脸，不假面感。人物身形为自然吸引力曲线，身形吸引力强度为中。雾蓝色短款吊带、白色轻薄开衫和浅色短裤形成清爽的冷色层次，服装完整自然，海风轻轻带动开衫边缘。柔和侧光让脖颈、锁骨、腰线、小腹和大腿线条自然呈现，整体是成年女性克制、有吸引力的身体线条表达，不低俗、不裸露、不情趣化。

镜头采用大腿以上构图，人物主体清晰，既保留脸部表情和服装层次，也自然呈现腰线与腿部比例。背景只保留远处海面、步道栏杆和傍晚天光，浅景深虚化，不喧宾夺主。整体采用冷白纯欲生活照滤镜，色温偏冷但不过曝，饱和度克制，肤色干净清透，画面真实、柔和、有电影生活剧照感。
```

---

## 4.5 负面约束写法规则

负面约束必须单独输出。

基础格式：

```text
避免未成年感、幼态脸、儿童化、过度少女化、欧美脸、网红整容脸、假面感、五官失衡、脸部僵硬、塑料皮肤、过度磨皮、低清晰度、强 AI 感、CG 插画感、肢体畸形、手部畸形、手指错误、身体比例失衡、姿势扭曲、服装结构混乱、服装变形、服装颜色错误、背景杂乱、主体不清、光线过曝、光线过暗、画面脏灰、廉价影楼风、低俗情色、裸露。
```
---

# 5. 只输出提示词模式

## 5.1 适用场景

当用户明确要求：

```text
只输出提示词
不要解释
直接给我最终提示词
输出可复制版即可
不用分析
```

使用只输出提示词模式。

---

## 5.2 输出格式

```text
【最终提示词】
……

【负面约束】
……
```

---

## 5.3 输出规则

此模式下：

```text
1. 不展示参数锁定结果；
2. 不解释路由过程；
3. 不写分析；
4. 不写推荐理由；
5. 但内部必须仍然执行参数锁定、路由、自检和安全边界。
```

---

# 6. 参数组合推荐模式

## 6.1 适用场景

当用户要求：

```text
推荐几组参数
给我几组吸睛组合
推荐 8 组
来几组爆款组合
不要只限室内
不要全是白色
场景不要重复
```

使用参数组合推荐模式。

---

## 6.2 输出格式

每组组合必须使用以下格式：

```text
序号：
组合名称：
写真风格：
场景方向：
服装方向：
气质标签：
五官方向：
身形方向：
身形吸引力强度：
线条重点：
镜头方向：
光线氛围：
滤镜效果：
画幅比例：
平台用途：
推荐理由：
```

---

## 6.3 推荐规则

推荐组合必须符合：

```text
1. 每组都可以直接复制给 Skill 调用；
2. 场景之间要有差异；
3. 服装颜色不要全部相同；
4. 气质不要全部重复；
5. 镜头方向可以有变化；
6. 光线和滤镜要与风格一致；
7. 不得推荐裸露、未成年感方向；
8. Curve 类组合必须明确成年女性和克制表达；
9. 电商类组合必须保留服装完整清晰；
10. 古风类组合必须保持服装结构和配色规则。
```

---

## 6.4 示例格式

```text
1.
组合名称：冷白海边纯欲生活照
写真风格：纯欲曲线生活照
场景方向：傍晚海边步道
服装方向：雾蓝色短款吊带 + 白色轻薄开衫 + 浅色短裤
气质标签：清纯、安静、克制、有吸引力
五官方向：清冷淡颜
身形方向：自然吸引力曲线
身形吸引力强度：中
线条重点：脖颈、锁骨、腰线、小腹、大腿
镜头方向：轻侧身站姿，大腿以上构图
光线氛围：傍晚柔和冷白自然光
滤镜效果：冷白纯欲生活照滤镜
画幅比例：9:16
平台用途：小红书种草 / X 传播
推荐理由：海边、冷白、短款穿搭和轻侧身构图容易形成高传播度，同时能保持克制和真实生活感。
```

---

# 7. 提示词优化模式

## 7.1 适用场景

当用户要求：

```text
优化这条提示词
帮我改得更稳定
不要改变原参数
让它更容易出图
这条提示词太机械了
```

使用提示词优化模式。

---

## 7.2 输出格式

```text
一、原提示词主要问题

1.
2.
3.

二、优化原则

1.
2.
3.

三、优化后提示词

……

四、负面约束

……
```

---

## 7.3 如果用户要求不要分析

当用户明确说：

```text
不用分析，直接输出优化版
```

使用：

```text
【优化后提示词】
……

【负面约束】
……
```

---

## 7.4 优化规则

优化必须：

```text
1. 保留原风格；
2. 保留原场景；
3. 保留原服装；
4. 保留原五官；
5. 保留原身形；
6. 保留原镜头；
7. 保留原光线；
8. 保留原滤镜；
9. 强化导演式扩写；
10. 强化成年边界；
11. 强化负面约束。
```

不得：

```text
1. 把用户原本的生活照改成影楼写真；
2. 把海边改成卧室；
3. 把短款吊带改成宽松卫衣；
4. 把清冷淡颜改成明艳浓颜；
5. 把大腿以上构图改成面部特写；
6. 为了安全直接删除用户原本的身形目标。
```

---

# 8. 失败诊断模式

## 8.1 适用场景

当用户要求：

```text
为什么出图不稳定
为什么没按参数生成
为什么服装跑偏
为什么脸都一样
为什么色差明显
为什么不像我想要的效果
帮我诊断一下
```

使用失败诊断模式。

---

## 8.2 输出格式

```text
一、主要问题

1.
2.
3.

二、为什么会影响出图

……

三、修改建议

1.
2.
3.

四、优化后提示词

……

五、负面约束

……
```

---

## 8.3 诊断维度

必须从以下方向检查：

```text
1. 参数是否冲突；
2. 风格是否混乱；
3. 路由是否错误；
4. 用户参数是否被替换；
5. 五官模块是否缺失；
6. 身形模块是否失控；
7. 服装结构是否不清；
8. 场景是否道具堆砌；
9. 姿势是否过复杂；
10. 镜头方向是否冲突；
11. 光线和滤镜是否冲突；
12. 平台用途是否覆盖主风格；
13. 负面约束是否不足；
14. 安全边界是否模糊。
```

---

## 8.4 简洁诊断格式

如果用户只想快速看原因，可使用：

```text
主要原因：
1.
2.
3.

最该先改：
……

修复版提示词：
……
```

---

# 9. 审查友好改写模式

## 9.1 适用场景

当用户要求：

```text
更容易通过审查
改成安全版
降低敏感
不要被拒
保留效果但更合规
GPT Image 2 审查友好
```

使用审查友好改写模式。

---

## 9.2 输出格式

```text
一、保留内容

风格：
场景：
服装：
气质：
镜头：
光线：
滤镜：
画幅：

二、风险改写

原表达：
安全表达：

三、审查友好提示词

……

四、负面约束

……
```

---

## 9.3 如果用户要求直接给安全版

使用：

```text
【审查友好提示词】
……

【负面约束】
……
```

---

## 9.4 改写原则

必须保留：

```text
1. 原风格；
2. 原场景；
3. 原服装；
4. 原构图；
5. 原光线；
6. 原滤镜；
7. 原气质；
8. 原画幅；
9. 原画面目标。
```

替换高风险词：

```text
性感 → 有吸引力 / 女性线条美 / 时尚写真感
诱惑 → 克制吸引力 / 情绪张力
暴露 → 轻盈穿搭 / 露肤克制 / 服装完整
丰满胸部 → 自然胸腰比例 / 身形曲线协调
挑逗 → 自然姿态 / 眼神克制
情趣 → 时尚内搭 / 生活化穿搭 / 高级私服
```

不得过度保守到画面目标消失。

---

# 10. 图片反推提示词模式

## 10.1 适用场景

当用户上传图片并要求：

```text
推导这张图的提示词
反推提示词
分析图片风格
根据图片写 AI 提示词
改成 3D CG
改成古风
保留构图但换服装
```

使用图片反推提示词模式。

---

## 10.2 输出格式

```text
一、图片风格分析

主体：
人物特征：
服装：
姿态：
场景：
光线：
色彩：
镜头：
氛围：

二、可复用提示词方向

……

三、最终提示词

……

四、负面约束

……
```

---

## 10.3 如果用户只要求输出提示词

使用：

```text
【最终提示词】
……

【负面约束】
……
```

---

## 10.4 反推规则

图片反推必须：

```text
1. 先分析图像中可见信息；
2. 区分必须保留与可以改写的部分；
3. 遵守用户明确修改要求；
4. 不机械复制原图；
5. 不照搬可能不稳定的细节；
6. 转化为目标风格下更稳定的提示词；
7. 保持成年边界和安全表达。
```

---

# 11. 多风格版本模式

## 11.1 适用场景

当用户要求：

```text
基于这条提示词生成 5 个不同风格版本
换几种风格
做几个方向
给我不同版本
```

使用多风格版本模式。

---

## 11.2 输出格式

```text
版本 1：
风格名称：
适合方向：
最终提示词：
负面约束：

版本 2：
风格名称：
适合方向：
最终提示词：
负面约束：
```

如果用户只是要参数组合，可以输出：

```text
版本 1：
组合名称：
写真风格：
场景方向：
服装方向：
气质标签：
五官方向：
身形方向：
镜头方向：
光线氛围：
滤镜效果：
画幅比例：
推荐理由：
```

---

## 11.3 多版本规则

必须保证：

```text
1. 每个版本风格差异明确；
2. 不改变用户锁定的核心参数；
3. 不把所有版本写成同一套句式；
4. 每个版本都要有独立画面意图；
5. 每个版本都要有对应 route 逻辑；
6. 不同版本之间场景、光线、滤镜或气质至少有一项明显差异。
```

---

# 12. Skill 文档输出模式

## 12.1 适用场景

当用户要求：

```text
继续输出核心文档
帮我写这个 Skill 文件
输出 route 文件
输出 core 文档
输出注册表
按结构重新设计
```

使用 Skill 文档输出模式。

---

## 12.2 输出格式

```text
# 文件路径 / 文件名

# 文档标题

版本编号：
文档类型：
适用范围：
核心职责：

---

正文……
```

---

## 12.3 输出规则

必须遵守：

```text
1. 一次只输出一个核心文档；
2. 不得只写摘要；
3. 不得和已经确定的 V1.4 规则冲突；
4. 不得擅自改变目录结构；
5. 不得把 route 具体母版写进 style-registry；
6. 不得把所有风格堆进 skill.md；
7. 不得把安全边界写得比 safety-boundary.md 更弱；
8. 不得删减参数锁定规则；
9. 不得出现 V2.0 / V2.1 作为当前主线版本；
10. 当前统一使用 FEMALE-PORTRAIT-DIRECTOR-V1.4。
```

---

## 12.4 文档结尾建议

每个核心文档结尾可以写：

```text
所有模块必须遵守本文件。
```

如果是在对话中连续输出文档，可以在文档外简短说明下一份建议输出的文件名。

---

# 13. 简短说明模式

## 13.1 适用场景

当用户只是询问概念、方向、结构判断时，可以使用简短说明。

例如：

```text
这些扩展包是不是模块化的？
这个结构是否合理？
这个文件应该放在哪里？
```

---

## 13.2 输出规则

简短说明可以不用完整文档格式，但必须：

```text
1. 直接回答问题；
2. 指出是否合理；
3. 给出关键建议；
4. 必要时指出风险；
5. 不输出无关大段模板。
```

---

# 14. 正向提示词与负面约束分离规则

正向提示词负责描述想要的画面。

应包含：

```text
人物主体
成年边界
五官方向
气质
身形与姿态
服装结构
动作链
场景空间
镜头构图
光线落点
滤镜质感
平台用途
```

负面约束负责排除问题。

应包含：

```text
未成年感
幼态化
低俗情色
裸露
情趣化
身体部位凝视
五官失衡
手部畸形
服装错误
背景杂乱
光线问题
低清晰度
强 AI 感
```

不得在正向提示词里反复堆负面词。

---

# 15. 输出长度控制

## 15.1 完整提示词

建议长度：

```text
参数锁定结果：中等长度
最终提示词：3–5 段
负面约束：1 段
```

不得为了简短删掉：

```text
五官
身形
服装
姿态
场景
镜头
光线
滤镜
成年边界
```

---

## 15.2 参数组合推荐

每组组合必须完整，不得只写：

```text
海边纯欲
咖啡馆清纯
港风复古
```

必须写成可调用参数。

---

## 15.3 Skill 文档

Skill 文档必须完整，不得只写摘要。

但一次只输出一个文档，避免多个核心文件混在一起。

---

# 16. 输出一致性规则

所有输出必须和以下文件保持一致：

```text
SKILL.md
skill/skill.md
skill/style-registry.md
skill/core/parameter-lock.md
skill/core/safety-boundary.md
skill/core/conflict-resolution.md
skill/core/fallback-rules.md
skill/references/director-expansion.md
skill/references/visual-libraries.md
```

不得出现以下不一致：

```text
1. 版本号写成 V2.0；
2. 把 V1.4 拆成多个主线版本；
3. 删除核心 5 个风格；
4. 删除新增 9 个重点风格；
5. 弱化成年边界；
6. 忽略五官模块；
7. 忽略身形模块；
8. 把平台用途当成主风格；
9. 让 overlay 替换 route；
10. 让 route 默认值覆盖用户参数。
```

---

# 17. 输出前自检

每次输出前必须检查：

```text
1. 当前任务类型是否识别正确？
2. 是否使用了正确输出模式？
3. 用户是否要求只输出提示词？
4. 用户是否要求完整文档？
5. 是否保留用户锁定参数？
6. 是否遵守成年边界？
7. 是否正向提示词与负面约束分离？
8. 是否输出了可直接复制内容？
9. 是否避免了摘要化？
10. 是否与 V1.4 已确定结构一致？
```

如果任意一项不合格，必须修正后再输出。

---

## 18. 最终原则

本文件的最终原则是：

```text
用户要提示词，就给可复制提示词；
用户要参数组合，就给完整可调用参数；
用户要优化，就给优化后的最终版本；
用户要诊断，就指出问题并给修复方案；
用户要安全改写，就保留效果并降低风险；
用户要反推，就分析后输出可用提示词；
用户要 Skill 文档，就一次输出一个完整核心文档。
```

输出格式必须服务于一个目标：

```text
让用户不需要二次整理，就可以直接使用。
```

所有模块必须遵守本文件。
